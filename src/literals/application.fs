module Aornota.Nebulous.Literals.Application

[<Literal>]
let APPLICATION_NAME = "nebulous"

// (pre-α | α) | *β* | γ | δ | ε) | ζ | η | θ | ι | κ | λ | μ | ν | ξ | ο | π | ρ) | σ | τ | υ | φ | χ | ψ | ω
[<Literal>]
let APPLICATION_VERSION = "β" // note: also update ..\..\README.md

[<Literal>]
let WINDOW_MINIMUM_WIDTH = 800.

[<Literal>]
let WINDOW_MINIMUM_HEIGHT = 600.
